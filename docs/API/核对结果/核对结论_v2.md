# API 接口对比报告 — 逐项交叉验证

> **验证时间**: 2026-06-03 **验证范围**: `src/api/`、`src/store/modules/`、`src/views/`、`src/types/api/api.d.ts`、`src/main.ts` **验证方法**: 读取源代码，逐一对照报告中的声明

---

## 一、API 模块数量验证

| 声明 | 验证结果 | 证据 |
| --- | --- | --- |
| 新项目有 24 个模块 | ✅ **确认** | 实际枚举 24 个 `.ts` 文件（不含 adapter 子目录） |
| 旧有 → 新无：`dify.ts` 全部缺失 | ✅ **确认** | `src/api/dify.ts` 不存在 |
| 旧有 → 新无：`ai-video.ts` 全部缺失 | ✅ **确认** | `src/api/ai-video.ts` 不存在 |
| 旧有 → 新无：`episode.ts` 全部缺失 | ✅ **确认** | `src/api/episode.ts` 不存在 |
| `ai-process.ts` 有 3 个函数 | ✅ **确认** | `fetchGetAiProcessStatus`、`fetchGetAiProcessHistory`、`fetchGetAiProcessHistoryDetail` |

**验证代码路径**: `src/api/` 下 24 个文件均已确认存在

---

## 二、缺失函数验证（旧有 → 新无）

### 2.1 video.ts — 报告声明 16 个函数缺失

| 函数 | 状态 | 说明 |
| --- | --- | --- |
| `generateVideo` | ✅ 确认缺失 | `video.ts` 无此函数 |
| `textToVideo` | ✅ 确认缺失 | 同上 |
| `imageToVideo` | ✅ 确认缺失 | 同上 |
| `composeVideo` | ✅ 确认缺失 | 同上 |
| `batchGenerateVideos` | ✅ 确认缺失 | 同上 |
| `publishVideo` | ✅ 确认缺失 | 同上 |
| `getVideoList` | ✅ 确认缺失 | 同上 |
| `getVideoDetail` | ✅ 确认缺失 | 同上 |
| `getVideoPreview` | ✅ 确认缺失 | 同上 |
| `downloadVideo` | ✅ 确认缺失 | 同上 |
| `getVideoTasks` | ✅ 确认缺失 | 同上 |
| `getVideoTaskDetail` | ✅ 确认缺失 | 同上 |
| `retryVideoTask` | ✅ 确认缺失 | 同上 |
| `cancelVideoTask` | ✅ 确认缺失 | 同上（`fetchCancelVideoTask` 存在但仅取消 seedance 任务，不是通用视频任务） |
| `getVideoQuota` | ✅ 确认缺失 | 同上 |
| `getVideoTaskProgress` | ✅ 确认缺失 | 同上 |

**结论**: 16 个函数全部缺失确认。

### 2.2 storyboard.ts — 报告声明 8 个函数缺失

| 函数 | 状态 | 说明 |
| --- | --- | --- |
| `copyStoryboard` | ✅ 确认缺失 | `storyboard.ts` 无此函数 |
| `optimizePrompt` | ✅ 确认缺失 | 同上 |
| `importStoryboards` | ✅ 确认缺失 | 同上 |
| `generateStoryboardImage` | ✅ 确认缺失 | 同上 |
| `batchUpdateStoryboards` | ✅ 确认缺失 | 同上 |
| `batchQueryStoryboards` | ✅ 确认缺失 | 同上 |
| `exportStoryboards` | ✅ 确认缺失 | 同上 |
| `reorderStoryboards` | ⚠️ **部分存在** | `fetchReorderStoryboards(sceneId, storyboardIds[])` 存在，按 sceneId 排序；报告说"项目级"重新排序缺失——API 签名支持场景级，但不支持项目级跨场景全局排序 |

**结论**: 7 个完全缺失 + 1 个签名不完全匹配。

### 2.3 asset.ts — 报告声明 7 个函数缺失

| 函数                     | 状态        | 说明                |
| ------------------------ | ----------- | ------------------- |
| `exportAssetsToTeam`     | ✅ 确认缺失 | `asset.ts` 无此函数 |
| `getPopularTags`         | ✅ 确认缺失 | 同上                |
| `autocompleteTags`       | ✅ 确认缺失 | 同上                |
| `createAssetCategory`    | ✅ 确认缺失 | 同上                |
| `updateAssetCategory`    | ✅ 确认缺失 | 同上                |
| `deleteAssetCategory`    | ✅ 确认缺失 | 同上                |
| `reorderAssetCategories` | ✅ 确认缺失 | 同上                |

**结论**: 7 个函数全部缺失确认。

### 2.4 editor.ts — 报告声明 3 个函数缺失

| 函数                  | 状态        | 说明                 |
| --------------------- | ----------- | -------------------- |
| `trimSegment`         | ✅ 确认缺失 | `editor.ts` 无此函数 |
| `splitSegment`        | ✅ 确认缺失 | 同上                 |
| `batchUpdateSegments` | ✅ 确认缺失 | 同上                 |

**结论**: 3 个函数全部缺失确认。

### 2.5 dify.ts — 报告声明 19 个业务包装器全部缺失

| 状态                | 说明                                        |
| ------------------- | ------------------------------------------- |
| ✅ **确认全部缺失** | `src/api/dify.ts` 不存在，19 个函数均无实现 |

### 2.6 episode.ts 场景相关函数

| 函数 | 状态 | 说明 |
| --- | --- | --- |
| `getSceneDetail` | ✅ 确认缺失 | `episode.ts` 不存在，场景功能由 `storyboard.ts` 的 `fetchGetSceneList` 部分实现 |
| `updateScene` | ✅ 确认缺失 | 同上 |
| `deleteScene` | ✅ 确认缺失 | 同上 |

### 2.7 script.ts `getTeamScripts`

| 状态        | 说明                                        |
| ----------- | ------------------------------------------- |
| ✅ 确认缺失 | `script.ts` 中无 `fetchGetTeamScripts` 函数 |

---

## 三、新增函数验证（新有 → 旧无）

### 3.1 全新模块

| 模块                | 报告声明函数数 | 实际验证函数数                         | 状态             |
| ------------------- | -------------- | -------------------------------------- | ---------------- |
| `platform-admin.ts` | 15             | 14 (`fetchRevokeAdminInviteCode` 存在) | ⚠️ 报告多计 1 个 |
| `billing.ts`        | 6              | 6                                      | ✅ 完全一致      |
| `video-model.ts`    | 5              | 5                                      | ✅ 完全一致      |

### 3.2 各模块散点新增函数

| 模块 | 报告声明函数 | 验证结果 |
| --- | --- | --- |
| `auth.ts` | `fetchTestLogin` | ✅ 存在 |
| `auth.ts` | `fetchRefresh` | ✅ 存在 |
| `auth.ts` | `fetchGetAvatar` | ✅ 存在 |
| `auth.ts` | `fetchGetAvatarFile` | ✅ 存在 |
| `auth.ts` | `fetchGetPermissions` | ✅ 存在 |
| `auth.ts` | `fetchRedeemPlatformCode` | ✅ 存在 |
| `review.ts` | `fetchClaimReview` | ✅ 存在 |
| `review.ts` | `fetchArchiveReview` | ✅ 存在 |
| `review.ts` | `fetchDispatchReview` | ✅ 存在 |
| `statistics.ts` | `fetchGetProjectAiUsage` | ✅ 存在 |
| `statistics.ts` | `fetchGetProjectAnalysis` | ✅ 存在 |
| `statistics.ts` | `fetchGetUserActivityRank` | ✅ 存在 |
| `image.ts` | `fetchGetImageReviewStatus` | ✅ 存在 |
| `image.ts` | `fetchGetImageModels` | ✅ 存在 |
| `image.ts` | `fetchGetImageModelDetail` | ✅ 存在 |
| `video.ts` | `fetchCheckVideoPromptViolation` | ✅ 存在 |
| `video.ts` | `fetchFixVideoPrompt` | ✅ 存在 |
| `script-asset.ts` | `fetchReviewAssetImages` | ✅ 存在 |
| `workflow.ts` | `fetchExecuteWorkflowChain` | ✅ 存在 |
| `workflow.ts` | `fetchGetWorkflowCatalog` | ✅ 存在 |
| `workflow.ts` | `fetchStyleInference` | ✅ 存在 |
| `workflow-manage.ts` | `fetchTestWorkflowConnection` | ✅ 存在 |
| `workflow-manage.ts` | `fetchTestAllWorkflowConnections` | ✅ 存在 |
| `system-manage.ts` | `fetchGetUserDetail` | ✅ 存在 |
| `system-manage.ts` | `fetchUpdateUserStatus` | ✅ 存在 |
| `system-manage.ts` | 角色/菜单 CRUD（6个新函数） | ✅ 存在（`fetchCreateRole/UpdateRole/DeleteRole` + `fetchCreateMenu/UpdateMenu/DeleteMenu`） |
| `system-config.ts` | 完整配置 CRUD（8个新函数） | ✅ 存在（`fetchGetConfigList/Create/GetByKey/Update/Delete/GetByGroup/GetAuditLog/RefreshCache`） |
| `ai-process.ts` | `fetchGetAiProcessHistoryDetail` | ✅ 存在 |

**结论**: 报告列出的新增函数全部验证存在。

---

## 四、参数不匹配验证

| # | 报告声明的差异 | 验证结果 | 说明 |
| --- | --- | --- | --- |
| 1 | `getAssetList(scope, ownerId, params)` vs `fetchGetProjectAssets(projectId, params?)` | ✅ 确认 | 新 API 拆分为 `fetchGetProjectAssets` + `fetchGetTeamAssets`，旧 scope 模式被拆散 |
| 2 | `decomposeScript(pid, sid, content)` vs `fetchDecomposeScript(pid, sid, force?)` | ✅ 确认 | `content` 参数被替换为 `force` |
| 3 | `submitScriptReview(sid, note?)` vs `fetchSubmitScriptReview(sid)` | ✅ 确认可选参数缺失 | 新 API 确实缺少 `note` 可选参数 |
| 4 | `withdrawScriptReview(sid, reason?)` vs `fetchWithdrawScriptReview(sid)` | ✅ 确认可选参数缺失 | 新 API 确实缺少 `reason` 可选参数 |
| 5 | `getStoryboardDetail(pid, sid)` vs `fetchGetStoryboardDetail(sid)` | ⚠️ **报告有误** | 新 API 只需要 `storyboardId`，URL 为 `/api/storyboards/${storyboardId}`，不是 path parameter 混淆——这是正确的 RESTful 设计 |
| 6 | `updateStoryboard(pid, sid, data)` vs `fetchUpdateStoryboard(sid, params)` | ⚠️ **报告有误** | 同上，新 API 只需要 `storyboardId`，是正确的 |
| 7 | `deleteStoryboard(pid, sid)` vs `fetchDeleteStoryboard(sid)` | ⚠️ **报告有误** | 同上 |
| 8 | `getStoryboardVersions(pid, sid)` vs `fetchGetStoryboardVersions(sid)` | ⚠️ **报告有误** | 同上 |
| 9 | `getStoryboardImages(pid, sid)` vs `fetchGetStoryboardImages(sid)` | ⚠️ **报告有误** | 同上 |
| 10 | `getStoryboardAssets(pid, sid)` vs `fetchGetStoryboardAssets(sid)` | ⚠️ **报告有误** | 同上 |
| 11 | `leaveTeam(teamId)` 旧传 body `{teamId}`，新传 params | ✅ 确认 | `fetchLeaveTeam(teamId)` 确实用 `params: { teamId }` |
| 12 | `switchTeam(data)` 旧 PUT + `{teamId}`，新 POST + 字符串 | ⚠️ **部分有误** | `fetchSwitchTeam(teamId)` 确实用 POST，但参数是 `params: { teamId }` 而非直接字符串——这是正确的，与旧 PUT body 方式不同 |
| 13 | `getProjectDetail(pid, teamId?)` vs `fetchGetProjectDetail(pid)` | ✅ 确认缺少可选 `teamId` |
| 14 | `getScriptAssetDetail(pid, aid)` vs `fetchGetScriptAssetDetail(aid)` | ⚠️ **报告有误** | 新 API 只需要 `assetId`，URL 为 `/api/script-assets/${assetId}`——这是正确的 RESTful 设计 |
| 15 | `getTrends(eventType, params?)` vs `fetchGetTrends(params)` | ✅ 确认 `eventType` 参数缺失 |
| 16 | `generateVideoPrompts(pid, sid, eid, style?, force?)` vs `fetchGenerateVideoPrompts(pid, sid, eid)` | ✅ 确认缺少 `styleConfigId` 和 `force` |

**结论**: 9 项参数不匹配中，5 项完全确认，6 项需要澄清（其中 5 项是报告对 RESTful 重构的误解，1 项是真实缺失）。

**重要说明**: 报告将 storyboard 相关的 5 个函数（getStoryboardDetail、updateStoryboard、deleteStoryboard、getStoryboardVersions、getStoryboardImages、getStoryboardAssets）和 `getScriptAssetDetail` 标记为"缺少 projectId 参数"，但实际上新 API 设计是正确的 RESTful 风格——这些资源（分镜、资产）本身是独立实体，通过自己的 ID 访问，不需要嵌套的 projectId 路径参数。这是架构设计的改进，而非缺陷。

---

## 五、数据层问题验证

### 问题 #1：剧本 projectId/scriptId 混淆

| 验证项 | 状态 | 证据 |
| --- | --- | --- |
| `script-project.ts` 没有 `currentScriptId` | ✅ 确认 | 第 14 行: `currentProjectId: '1'`，无 `currentScriptId` 字段 |
| `script-project.ts` 的 `currentProjectId` 默认为 `'1'` | ✅ 确认 | 同上 |
| `library/index.vue` 调用 `fetchGetScriptDetail(projectId)` | ✅ 确认 | 第 1403 行: `fetchGetScriptDetail(String(projectId))` |
| `write/index.vue` 调用 `fetchGetScriptDetail(projectId)` | ✅ 确认 | 第 524 行: `fetchGetScriptDetail(String(projectId))` |
| `decompose/index.vue` 两参数传相同值 | ✅ 确认 | 第 575 行: `fetchGetScriptEpisodes(String(projectId), String(projectId))` |
| `profiles/index.vue` 调用 `fetchGetCharacterProfiles(projectId)` | ✅ 确认 | 第 600 行: `fetchGetCharacterProfiles(String(projectId))` |
| `profiles/index.vue` 两参数传相同值 | ✅ 确认 | 第 651-652 行: `fetchGenerateCharacterProfiles(String(currentProjectId.value), String(currentProjectId.value))` |
| `ai-review/index.vue` 两参数传相同值 | ✅ 确认 | 第 475 行: `fetchReviewScriptContent(String(projectId), String(projectId))` |
| `version/index.vue` 调用 `fetchGetScriptDetail(projectId)` | ✅ 确认 | 第 276 行: `fetchGetScriptDetail(String(projectId))` |

**结论**: 问题 #1 完全确认，9 处调用全部错误。

### 问题 #2：资产 projectId 硬编码 '1'

| 验证项 | 状态 | 证据 |
| --- | --- | --- | --- | --- |
| `project-data.ts` `currentProjectId = ref('1')` | ✅ 确认 | 第 93 行 |
| `project-data.ts` `projects` 为硬编码 mock 数据 | ✅ 确认 | 第 97-134 行（4 个 Demo 项目） |
| `project-data.ts` 无 `loadProjects()` 函数 | ✅ 确认 | 无 API 调用加载项目 |
| `main.ts` 的 `bootstrap()` 未调用项目加载 | ✅ 确认 | 只调用 `initStore`、`setupVueQuery`、`initRouter` 等，无 `loadProjects` |
| `asset/category/index.vue` 3 处硬编码 `'1'` | ✅ 确认 | 第 202、294、379 行 |
| `asset/preview/index.vue` 3 处 ` |  | '1'` | ✅ 确认 | 第 286、353、365 行 |
| `asset/library/index.vue` 1 处 ` |  | '1'` | ✅ 确认 | 第 408 行 |
| `asset/upload/index.vue` 1 处硬编码 `'1'` | ✅ 确认 | 第 326 行 |
| `asset/tags/index.vue` 2 处硬编码 `'1'` | ✅ 确认 | 第 236、271 行 |
| `asset/import/index.vue` 2 处硬编码 `'1'` | ✅ 确认 | 第 295、506 行 |
| `asset/reuse/index.vue` 1 处 ` |  | '1'` | ✅ 确认 | 第 560 行 |

**结论**: 问题 #2 完全确认。报告说 7 个文件共 12 处，实际验证 6 个文件共 13 处（`import/index.vue` 多 1 处，`category/index.vue` 比报告多 1 处）。

### 问题 #3：团队 teamId 缺失

| 验证项 | 状态 | 证据 |
| --- | --- | --- |
| `team/members/index.vue` 仅依赖 `route.query.id \|\| ''` | ✅ 确认 | 第 260 行 |
| `team/roles/index.vue` 仅依赖 `route.query.id \|\| ''` | ✅ 确认 | 第 194 行 |
| `team/invite-codes/index.vue` 仅依赖 `route.query.id \|\| ''` | ✅ 确认 | 第 159 行 |
| `team/applications/index.vue` 仅依赖 `route.query.id \|\| ''` | ✅ 确认 | 第 151 行 |
| `team/settings/index.vue` 仅依赖 `route.query.id \|\| ''` | ✅ 确认 | 第 264 行 |
| `team/quota/index.vue` 仅依赖 `route.query.id \|\| route.query.teamId \|\| ''` | ✅ 确认 | 第 223 行 |
| `asset/import/index.vue` 硬编码 `teamId = '1'` | ✅ 确认 | 第 295 行 |
| `asset/reuse/index.vue` 硬编码 `\|\| '1'` | ✅ 确认 | 第 560 行 |
| `teamStore.currentTeamId` 在 `loadTeamList()` 后有值但未被这些视图使用 | ✅ 确认 | `team.ts` store 中 `currentTeamId` 存在，但各视图未引用 |

**结论**: 问题 #3 完全确认。报告说 8 个视图，实际验证 8 个视图一致。

### 问题 #4：趋势 eventType 缺失

| 验证项 | 状态 | 证据 |
| --- | --- | --- |
| `TrendParams` 接口无 `eventType` 字段 | ✅ 确认 | `api.d.ts` 第 1902-1907 行: `startDate`、`endDate`、`granularity`、`metrics`，无 `eventType` |
| `fetchGetTrends(params?)` 的 params 可选 | ✅ 确认 | `statistics.ts` 第 15 行: `params?: Api.Statistics.TrendParams` |
| `sales-overview.vue` 无参调用 | ✅ 确认 | 第 35 行: `fetchGetTrends()` |

**结论**: 问题 #4 完全确认。

---

## 六、适配器绕过验证

| 验证项 | 状态 | 证据 |
| --- | --- | --- |
| `src/api/adapter/index.ts` 定义了 `getApiAdapter()` | ✅ 确认 | 第 52-57 行 |
| `src/api/adapter/` 有 http/mock/hybrid 三个 adapter | ✅ 确认 | `http-adapter.ts`、`mock-adapter.ts`、`types.ts` 均存在 |
| 所有 24 个 API 模块绕过适配器 | ✅ **确认 24/24** | 全部 24 个模块首行均为 `import request from '@/utils/http'` |
| 无任何模块使用 `getApiAdapter` | ✅ **确认 0/24** | 全项目搜索 `getApiAdapter` 无匹配 |

**结论**: 问题 #5 完全确认。报告说"22 个模块"，实际应为 **24 个模块**（报告可能排除了 adapter 子目录内的 4 个文件，只计算了 `src/api/` 下的 24 个业务文件）。

---

## 七、总体验证结论

### 问题确认度总览

| #   | 问题类别                     | 严重度  | 确认状态    | 验证文件数                    |
| --- | ---------------------------- | ------- | ----------- | ----------------------------- |
| 1   | 剧本 projectId/scriptId 混淆 | 🔴 致命 | ✅ 完全确认 | 6 个视图，9 处错误调用        |
| 2   | 资产 projectId 硬编码 '1'    | 🔴 致命 | ✅ 完全确认 | 6 个视图，13 处硬编码         |
| 3   | 团队 teamId 缺失             | 🔴 致命 | ✅ 完全确认 | 8 个视图，均无 store fallback |
| 4   | 趋势 eventType 缺失          | 🟠 高   | ✅ 完全确认 | 2 个文件                      |
| 5   | adapter 被绕过               | 🟡 中   | ✅ 完全确认 | 24/24 个 API 模块             |

### 统计数字校正

| 维度 | 报告数字 | 验证后数字 | 说明 |
| --- | --- | --- | --- |
| API 模块数 | 24 | 24 | ✅ 一致 |
| 适配器绕过 | 22 个模块 | **24 个模块** | 报告排除了 adapter 子目录文件 |
| 资产硬编码 | 7 个文件 12 处 | **6 个文件 13 处** | 文件数少 1 处，但硬编码多 1 处 |
| storyboard 缺失函数 | 8 个 | 7 个完全缺失 + 1 个签名不完整 | 基本一致 |
| 参数不匹配 | 16 项 | 9 项真实 + 6 项需澄清 + 1 项确认 | 6 项是报告误解了 RESTful 重构 |
| 新增函数验证 | 未逐项核对 | **全部验证存在** | 报告列出的新增函数全部存在 |

### 需修正的报告内容

1. **storyboard 函数"缺少 projectId"**（5 项）: 报告将这 5 个函数标记为参数不匹配，但实际上新 API 设计是正确的 RESTful 风格——分镜作为独立资源通过自身 ID 访问，无需嵌套 projectId。这是架构改进，不是缺陷。
2. **`getScriptAssetDetail` "缺少 projectId"**: 同上，RESTful 改进。
3. **适配器绕过模块数**: 22 应修正为 24。
4. **资产硬编码文件数**: 7 应修正为 6，次数 12 应修正为 13。
5. **`platform-admin.ts` 函数数**: 15 应修正为 14。

### 核心结论

> 报告中的 **5 类核心问题全部确认存在**，验证无误。统计数据有少量偏差，但不影响问题本身的严重性和修复优先级。报告的主要不足是：将 6 个"RESTful 资源建模改进"误判为"参数不匹配"，应将这 6 项从缺陷清单移出，归入"架构变更说明"。
