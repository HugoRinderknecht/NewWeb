# 项目管理模块合并分析报告

## 一、模块概览

项目管理模块目前包含 **10 个子模块**，位于 `src/views/project/` 目录下：

| 序号 | 模块 | 文件路径 | 功能定位 |
|------|------|----------|----------|
| 1 | 项目列表 | `list/index.vue` | 项目总览、搜索筛选、批量操作 |
| 2 | 创建项目 | `create/index.vue` | 三步弹窗创建项目 |
| 3 | 编辑项目 | `edit/index.vue` | 项目详情页（7个Tab） |
| 4 | 项目成员 | `member/index.vue` | 成员邀请、角色管理 |
| 5 | 角色管理 | `characters/index.vue` | 项目角色/人物库管理 |
| 6 | 剧本管理 | `scripts/index.vue` | 剧本创建、上传、编辑 |
| 7 | 集数管理 | `episodes/index.vue` | 集数与镜头管理 |
| 8 | 项目统计 | `statistics/index.vue` | 数据看板与进度统计 |
| 9 | 项目设置 | `settings/index.vue` | 基础配置、审核门禁、JSON配置 |
| 10 | 归档删除 | `delete/index.vue` | 归档与删除管理 |

---

## 二、可合并模块分析

### 2.1 第一组：`create` + `edit` 的"概览"Tab → 合并为统一表单

#### 现状对比

| 维度 | `create/index.vue` | `edit/index.vue` 概览Tab |
|------|-------------------|-------------------------|
| **形式** | 弹窗组件（被list引用） | 页面内Tab |
| **字段** | 名称、描述、类型、负责人、封面 | 名称、描述、类型、负责人、状态、进度 |
| **交互** | 三步向导（模板→信息→完成） | 直接表单编辑 |
| **代码量** | ~290行 | ~80行（仅概览部分） |

#### 合并理由

1. **功能高度重合**：两者都维护项目的基础信息字段（名称、描述、类型、负责人、封面）
2. **已存在引用关系**：`list/index.vue` 第233行已直接 `import ProjectCreateDialog from '../create/index.vue'`
3. **数据模型一致**：两者使用相同的 `ProjectType` 枚举和状态映射
4. **维护成本高**：创建和编辑的表单验证规则分散在两处，修改需同步

#### 合并方案

- 将 `create` 的三步向导逻辑整合进 `edit` 的"概览"Tab
- 或提取为独立的 `ProjectForm` 组件，供创建弹窗和编辑页面共用
- 创建时隐藏"状态/进度"字段，编辑时显示全部字段

---

### 2.2 第二组：`settings` → 并入 `edit` 的"设置"Tab

#### 现状对比

| 维度 | `settings/index.vue`（独立页面） | `edit/index.vue` 设置Tab |
|------|-------------------------------|-------------------------|
| **字段** | 状态、名称、描述、审核门禁、小组配置、JSON配置 | 封面、公开、通知、自动归档、归档/删除按钮 |
| **定位** | 全局项目配置中心 | 项目详情页内的一个Tab |
| **代码量** | ~230行 | ~50行 |

#### 合并理由

1. **功能完全重叠**：两者都是操作同一项目的配置数据
2. **字段互补**：`edit` 的设置Tab缺少审核门禁、小组配置、JSON配置；`settings` 缺少封面上传
3. **入口分散**：用户可能在两个地方找到功能相似但不同的设置页面，造成困惑
4. **数据同步风险**：独立页面与详情页Tab若数据不同步，会导致体验不一致

#### 合并方案

- 将 `settings` 的全部表单内容（基础配置、审核门禁、小组配置、JSON配置）迁移到 `edit` 的"设置"Tab
- 补充封面上传字段到合并后的设置Tab
- 删除独立的 `settings` 页面及其路由

---

### 2.3 第三组：`delete` → 并入 `list`

#### 现状对比

| 维度 | `delete/index.vue` | `list/index.vue` |
|------|-------------------|------------------|
| **数据** | mockProjectList（6条） | mockProjectList（8条） |
| **UI** | 表格 + 搜索 + 筛选 + 批量操作 | 表格/卡片 + 搜索 + 筛选 + 批量操作 |
| **功能** | 归档、恢复、删除、批量操作 | 查看、编辑、删除、批量归档/删除 |
| **代码量** | ~400行 | ~600行 |

#### 合并理由

1. **数据同源**：两者都维护项目列表数据，状态枚举完全一致
2. **UI同质**：都是项目列表表格，列定义几乎相同
3. **功能可覆盖**：`delete` 的"恢复"功能可通过在 `list` 中增加"已归档"状态筛选 + 恢复按钮实现
4. **避免重复**：`delete` 的 mock 数据与 `list` 高度重复，只是少了部分字段

#### 合并方案

- 在 `list` 的状态筛选中增加"已归档"选项
- 在操作列中增加"恢复"按钮（仅对已归档项目显示）
- 在批量操作中增加"批量恢复"选项
- 删除独立的 `delete` 页面及其路由

---

## 三、不可合并模块（保持独立）

| 模块 | 保持独立理由 |
|------|-------------|
| `list` | 核心入口页面，承载项目总览与导航，无法被替代 |
| `member` | 功能独立（成员邀请/角色管理），且已被 `edit` 作为Tab组件引用，保持独立便于复用 |
| `characters` | 独立业务实体（角色库），字段（性别、年龄、性格、外貌、背景）与项目其他模块无交集 |
| `scripts` | 独立业务实体（剧本管理），字段（字数、集数、作者、状态）和流程独特 |
| `episodes` | 独立业务实体（集数+镜头管理），左右分栏交互复杂，与其他模块无重叠 |
| `statistics` | 纯展示型统计看板，与其他模块的功能无交集，数据只读 |

---

## 四、合并收益评估

| 合并项 | 减少文件数 | 减少冗余代码 | 维护收益 | 风险等级 |
|--------|-----------|-------------|---------|---------|
| `create` → 并入 `edit/form` | 1 | 表单逻辑、类型定义统一 | 中 | 低 |
| `settings` → 并入 `edit` | 1 | 配置字段、状态映射统一 | 高 | 低 |
| `delete` → 并入 `list` | 1 | 表格逻辑、mock数据统一 | 高 | 低 |
| **合计** | **3个文件** | 避免多处重复的状态映射与mock数据 | — | — |

---

## 五、建议的合并后目录结构

```
src/views/project/
├── list/                    # 项目列表（增强：支持恢复已归档项目）
│   └── index.vue
├── edit/                    # 项目详情（Tab容器）
│   ├── index.vue
│   └── tabs/
│       ├── overview.vue     # 概览（统一create/edit表单逻辑）
│       ├── script.vue       # 剧本（原edit内嵌逻辑提取）
│       ├── storyboard.vue   # 分镜
│       ├── video.vue        # 视频
│       ├── asset.vue        # 资产
│       ├── member.vue       # 成员（引用member组件）
│       └── settings.vue     # 设置（原settings全部内容迁移）
├── member/                  # 成员管理（保持独立，被edit引用）
│   └── index.vue
├── characters/              # 角色管理
│   └── index.vue
├── scripts/                 # 剧本管理
│   └── index.vue
├── episodes/                # 集数管理
│   └── index.vue
└── statistics/              # 项目统计
    └── index.vue
```

---

## 六、实施建议

### 6.1 实施顺序（推荐）

1. **第一步**：`settings` → 并入 `edit`
   - 风险最低，纯内容迁移
   - 删除独立路由即可

2. **第二步**：`delete` → 并入 `list`
   - 增加"已归档"筛选和"恢复"操作
   - 删除独立路由

3. **第三步**：`create` → 提取为共用组件
   - 将 `create` 的表单逻辑提取为 `ProjectForm`
   - `edit` 的"概览"Tab和 `list` 的创建弹窗共用此组件

### 6.2 注意事项

- **路由调整**：合并后需同步更新路由配置文件，删除被合并页面的路由定义
- **菜单调整**：若侧边栏菜单有独立入口，需移除被合并模块的菜单项
- **数据mock统一**：`list` 和 `delete` 的mock数据需合并为一份，避免数据不一致
- **组件引用更新**：`edit` 中引用 `member` 组件的路径需确认无需变更

---

## 七、结论

项目管理模块的10个子模块中，**`create`、`settings`、`delete` 三个模块建议合并**，可减少3个独立文件，消除多处重复的状态映射与mock数据，降低后续维护成本。其余7个模块因功能独立、业务实体不同，建议保持现状。

---

*报告生成时间：2026-05-26*
*分析范围：src/views/project/ 目录下全部模块*
